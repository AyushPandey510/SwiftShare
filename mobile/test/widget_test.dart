import 'package:flutter_test/flutter_test.dart';
import 'package:swiftshare_mobile/main.dart';

void main() {
  test('SwiftShare app widget can be constructed', () {
    expect(const SwiftShareApp(), isA<SwiftShareApp>());
  });
}
